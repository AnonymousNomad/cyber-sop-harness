# Phase Plan Index

The authoritative phase plan is `ROADMAP.md`. This index exists so the governance skill's required `phase-plan` artifact has a stable path.

Current phase: Phase 3B, local model runtime and provider selection
Phase status: Research complete; implementation not started; live-provider execution remains blocked
Next phase: Phase 4 methodology and SOP engine
Transition authority: Phase gate in `ROADMAP.md`, acceptance matrix in `docs/acceptance-matrix.md`, and audit event in `agent_notes.Md`
